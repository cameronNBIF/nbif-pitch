# Azure Functions SDK
azure-functions

# Azure Storage (Blob for archive, Table for dedup, Queue for dead-letter)
azure-storage-blob
azure-storage-queue
azure-data-tables

# HTTP requests (Affinity API, Turnstile verification)
requests

# URL-safe slug generation for blob paths
python-slugify

# Code cleanup and formatting
black
flake8

# Slack SDK for notifications
slack_sdk